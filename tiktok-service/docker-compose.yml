services:
  clipforge-tiktok:
    build: .
    container_name: clipforge-tiktok
    restart: unless-stopped
    ports:
      - "8100:8100"
    env_file:
      - .env
    dns:
      - 8.8.8.8
      - 8.8.4.4
    volumes:
      - tiktok_data:/app/data

volumes:
  tiktok_data:
